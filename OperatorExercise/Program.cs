namespace OperatorExercise
{
    public class Program
    {
        static void Main(string[] args)
        {
            int a = 17; 
            int b = 4;

            int sum = a + b;
            int difference = a - b;
            int product = a * b;

            string resultMessage = $"Sum: {sum}, " + $"Difference: {difference}, " + $"Product: {product} ";
            
            Console.WriteLine(resultMessage);
            
            int quotient = a / b;
            int remainder = a % b;
            
            string resultMessage2 = $"{a}/{b} is {quotient} remainder {remainder}";
            
            Console.WriteLine(resultMessage2);
            
            
            //Waiting on 1-1 to Complete full exercise
        }
        
        static double AreaOfCircle
    }
}
